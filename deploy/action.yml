name: 'Deploy Compute@Edge Package'
description: 'Deploys the packaged binary to a Compute@Edge service.'
runs:
  using: 'node12'
  main: 'index.js'
inputs:
  project_directory:
    description: 'Directory of the project to build, relative to the repository root.'
    required: false
    default: './'